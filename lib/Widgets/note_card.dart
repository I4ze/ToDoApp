import 'package:flutter/material.dart';

Widget noteCard(Function()? onTap) {
  return InkWell(
    onTap: onTap,
    child: Container(
      padding: EdgeInsets.all(8.0),
      margin: EdgeInsets.all(8.0),
      decoration: ,
    ),

  );
}
